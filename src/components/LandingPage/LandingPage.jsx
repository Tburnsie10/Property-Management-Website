import React, { useState } from 'react';
import { useHistory } from 'react-router-dom';
import './LandingPage.css';

// CUSTOM COMPONENTS
import RegisterForm from '../RegisterForm/RegisterForm';
import LandingPageImg from '../LandingPageImg/LandingPageImg';

function LandingPage() {
  const [heading, setHeading] = useState('Welcome');
  const history = useHistory();

  const onLogin = (event) => {
    history.push('/login');
  };

  return (
    <div className="container">
      <h2>{heading}</h2>

      <div className="grid">
        <div className="grid-col grid-col_8">
          <p>
            This is a co-contributing project that has the goal of building a functional property Management 
            website for renting clients to access their portal, pay their bill, submit maintenance requests and
            feel at home with their current rental property.
          </p>

          <p>
            Contributors are Tyler Burns and Nicholas Dinis.        
          </p>

          <p>
            <LandingPageImg></LandingPageImg>
          </p>
        </div>
        <div className="grid-col grid-col_4">
          <RegisterForm />

          <center>
            <h4>Already a Member?</h4>
            <button className="btn btn_sizeSm" onClick={onLogin}>
              Login
            </button>
          </center>
        </div>
      </div>
    </div>
  );
}

export default LandingPage;
